# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# Rust build artifacts — regenerated by `cargo build`, never commit (and they blow past GitHub's 100MB limit)
target/
**/target/
*.pdb
*.rlib
*.rmeta
# (Cargo.lock IS committed for binary crates — reproducible builds — so it is NOT ignored)

# Local-only — never commit the sealed workbench or secrets
Reality_Engine/
*.key
*.pem
*.secret
CHRONICLE_SIGNER_PASSPHRASE

demo_metrics.jsonl
demo_convergence.png
__pycache__/
*.pyc
