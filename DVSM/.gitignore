# Generated by verify.py's live commercial gate (Obligation B) — a per-run build receipt, not source.
# receipt ≠ proof; it records that a suite ran+passed in one build, regenerated each run.
.verify_receipt.tsv
